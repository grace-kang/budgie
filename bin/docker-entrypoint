#!/bin/bash
set -e

# Remove stale PID
rm -f /rails/tmp/pids/server.pid

# Enable jemalloc
if [ -z "${LD_PRELOAD+x}" ]; then
export LD_PRELOAD=$(find /usr/lib -name libjemalloc.so.2 -print -quit)
	fi

# Just execute whatever command comes in
	exec "$@"
